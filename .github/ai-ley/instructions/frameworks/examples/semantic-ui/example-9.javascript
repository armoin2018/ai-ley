// Initialize dropdowns
$('.ui.dropdown').dropdown();

// Initialize modals
$('.ui.modal').modal();

// Initialize tabs
$('.tabular.menu .item').tab();

// Custom settings
$('.ui.modal').modal({
  blurring: true,
  transition: 'scale',
});