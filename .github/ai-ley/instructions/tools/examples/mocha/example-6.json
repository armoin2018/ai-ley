{
  "spec": ["test/**/*.test.ts", "test/**/*.spec.ts"],
  "require": ["ts-node/register", "tsconfig-paths/register", "test/setup.ts"],
  "extensions": ["ts"],
  "timeout": 10000,
  "recursive": true,
  "reporter": "spec",
  "exit": true
}